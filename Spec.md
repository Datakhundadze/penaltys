# SPOT — ონლაინ პენალტების თამაში
## ტექნიკური და დიზაინის სპეციფიკაცია v1.0

---

## 1. რა ვაშენებთ

1v1 ონლაინ პენალტების თამაში ბრაუზერში. მოთამაშე ტვირთავს სელფის და იღებს საკუთარ 3D ავატარს. ურტყამს და იცავს კარს რეალური მოწინააღმდეგის წინააღმდეგ, აგროვებს გამოცდილებას ვარჯიშის რეჟიმში, ზრდის სტატებს და ადის ლიგებში.

**ძირითადი ციკლი:** ვარჯიში → სტატების ზრდა → ონლაინ მატჩი → MMR → ლიგა → უფრო ძლიერი მოწინააღმდეგე.

**MVP-ის განმარტება:** ორი მოთამაშე ოთახის კოდით ხვდება ერთმანეთს, თამაშობს 5+5 პენალტს, შედეგი ინახება. ავატარი, ლიგები და ვარჯიში ამის შემდეგ ემატება.

---

## 2. ტექნოლოგიური სტეკი

| ფენა | არჩევანი | რატომ |
|---|---|---|
| Frontend | Vite + React + TypeScript | Cloudflare Pages-ზე ადაპტერების გარეშე ჯდება; სერვერული ფრეიმვორკი არ გვჭირდება |
| 3D | React Three Fiber + drei | Three.js React-ულად; ავატარები glTF-ია |
| ფიზიკა | საკუთარი, მარტივი | სრული ფიზიკის ძრავა აქ ზედმეტია — იხ. §5 |
| Auth / DB / Storage | Supabase | უკვე გაქვს |
| Realtime | Supabase Realtime (Broadcast + Postgres Changes) | ცალკე WS სერვერი არ გჭირდება |
| ავტორიტეტული ლოგიკა | Supabase Edge Functions (Deno) | აქ იწერება შედეგი — არასდროს კლიენტზე |
| ავატარი | გარე selfie→glTF სერვისი | იხ. §7 |
| Hosting | Cloudflare Pages ← GitHub | ყოველი merge main-ში ავტომატურად deploy-დება |
| Workflow | Claude Code cloud სესიები | GitHub რეპოზე, ტერმინალის გარეშე; შედეგი PR-ებად მოდის |

**რატომ არა ცალკე Node/Socket.IO სერვერი:** პენალტი არ არის უწყვეტი რეალურ-დროის თამაში. რაუნდში ხდება ზუსტად ორი რამ — ორივე მოთამაშე აგზავნის ფარულ არჩევანს, სერვერი აბრუნებს შედეგს. ეს Edge Function-ით სრულყოფილად იხურება და ერთი სერვისით ნაკლებს მოვლი.

---

## 3. მონაცემთა სქემა (Postgres)

```sql
-- პროფილი
profiles (
  id            uuid pk references auth.users,
  username      text unique not null,
  level         int default 1,
  xp            int default 0,
  mmr           int default 1000,
  league        text default 'bronze',
  energy        int default 10,
  energy_at     timestamptz default now(),
  created_at    timestamptz default now()
)

-- სტატები (ცალკე, რომ მატჩის დროს მარტივად წავიკითხოთ)
player_stats (
  user_id   uuid pk references profiles,
  power     int default 20,  accuracy int default 20,
  curve     int default 20,  nerve    int default 20,
  reflex    int default 20,  reach    int default 20,
  hands     int default 20,  reading  int default 20,
  unspent   int default 0
)

-- ავატარი
avatars (
  user_id     uuid pk references profiles,
  glb_url     text,
  thumb_url   text,
  status      text,          -- pending | ready | rejected
  moderated   boolean default false,
  created_at  timestamptz
)

-- მატჩი
matches (
  id          uuid pk,
  player_a    uuid, player_b uuid,
  mode        text,          -- friendly | ranked
  room_code   text unique,
  status      text,          -- waiting | active | finished | abandoned
  score_a     int default 0, score_b int default 0,
  winner      uuid,
  created_at  timestamptz, finished_at timestamptz
)

-- რაუნდი (საჯარო ნაწილი)
rounds (
  id          uuid pk,
  match_id    uuid references matches,
  idx         int,
  shooter     uuid,
  seed        bigint,
  result      jsonb,         -- null სანამ არ გადაწყდა
  deadline    timestamptz,
  resolved_at timestamptz
)

-- ფარული არჩევანი — SELECT პოლიტიკა საერთოდ არ აქვს
round_inputs (
  round_id  uuid references rounds,
  user_id   uuid,
  payload   jsonb,
  role      text,            -- shooter | keeper
  created_at timestamptz,
  primary key (round_id, user_id)
)

-- ვარჯიში
drills (id, code, title, description, xp_reward, stat_reward, min_level)
drill_attempts (id, user_id, drill_id, score, passed, created_at)

-- სეზონი
seasons (id, name, starts_at, ends_at, active)
season_results (season_id, user_id, final_mmr, final_league, rewards jsonb)
```

### RLS — ეს არის ანტი-ჩიტის გული

```sql
-- round_inputs: არავითარი SELECT არავისთვის.
-- მხოლოდ service_role (Edge Function) კითხულობს.
alter table round_inputs enable row level security;
-- (SELECT პოლიტიკა განზრახ არ იქმნება)

-- rounds: ხედავ მხოლოდ იმ მატჩების რაუნდებს, სადაც თამაშობ
create policy "own matches" on rounds for select using (
  exists (select 1 from matches m where m.id = rounds.match_id
          and (m.player_a = auth.uid() or m.player_b = auth.uid()))
);

-- player_stats: სხვისი სტატები ჩანს, მაგრამ UPDATE მხოლოდ სერვერიდან
create policy "read all" on player_stats for select using (true);
-- UPDATE პოლიტიკა არ არსებობს — მხოლოდ Edge Function
```

⚠️ თუ სტატების განახლებას კლიენტს მიანდობ, პირველივე კვირაში 100/100 სტატებიანი მოთამაშეები გეყოლება. ყველა ჩაწერა, რაც პროგრესზე ან შედეგზე მოქმედებს, Edge Function-ში უნდა იყოს.

---

## 4. რაუნდის პროტოკოლი

```
1. სერვერი ქმნის რაუნდს: seed, deadline = now + 12s
   → Realtime broadcast: round:start { idx, shooter, deadline }

2. ორივე მოთამაშე აგზავნის არჩევანს Edge Function-ში: submit-input
   დამრტყმელი: { aim: {x, y}, power, timing }
   მეკარე:     { dive: {x, y}, commit_at }
   ფუნქცია წერს round_inputs-ში. სხვა მოთამაშე ვერაფერს ხედავს.

3. როცა ორივე ჩავიდა (ან deadline გავიდა → შემთხვევითი არჩევანი):
   ფუნქცია ითვლის შედეგს seed-ით
   → broadcast: round:resolve { shooter_input, keeper_input, seed, result }

4. კლიენტი აანიმაციებს დეტერმინისტულად იმავე seed-ით.
   ანიმაცია არაფერს წყვეტს — შედეგი უკვე მიღებულია.
```

**კრიტიკული:** კლიენტი ანიმაციას თამაშობს, არა თამაშს. ეს ერთდროულად ხსნის დესინქრონსაც და მოტყუებასაც.

---

## 5. თამაშის ფორმულები

კარი ნორმალიზებულ კოორდინატებში: `x ∈ [-1, 1]` (ბოძიდან ბოძამდე), `y ∈ [0, 1]` (მიწიდან ჰორიზონტალამდე).

### დარტყმა

```
შემავალი: aim(x,y) swipe-იდან, power ∈ [0,1], timing ∈ [0,1] (ზოლის ხარისხი)

გაფანტვა:
  σ = 0.22 × (1 − ACC/150) × (1 + 0.5×(power − 0.5)) × (1.4 − 0.4×timing)

დაშვების წერტილი:
  landing = aim + gauss(0, σ)   ← seed-იდან, ორივე მხარეს იგივე

თუ |landing.x| > 1.0 ან landing.y > 1.0 → აუტი
თუ 0.95 < |landing.x| ≤ 1.0            → ბოძი (ტკბილი მომენტი, არ მოაკლდეს ხმა)

ბურთის ფრენის დრო:
  v = 18 + 0.12 × POWER          (მ/წმ)
  t_ball = 12.2 / v              (≈0.68წმ სუსტი → ≈0.40წმ მაქსიმალური)

CURVE ამრუდებს ტრაექტორიას — მეკარის დივი წერტილთან უფრო შორს აღმოჩნდება,
თუ მან ადრე დაიწყო.
```

### გადარჩენა

```
d = |dive − landing|             (კარის ერთეულებში)
R = 0.28 + REACH/400             (წვდომის რადიუსი)

თუ d > R → გოლი

წინააღმდეგ შემთხვევაში:
  p_save = 0.90 − 0.60×(d/R) − 0.20×(POWER/100) + REFLEX/500
  p_save clamp [0.05, 0.95]

  თუ მეკარემ ადრე დაიწყო (commit_at < t_ball − 0.25):
    p_save −= 0.15               ← წინასწარ დაწოლის ჯარიმა

rebound: თუ გადაარჩინა და random > HANDS/100 → ბურთი ბრუნდება თამაშში
```

### READING სტატი

მეკარე ხედავს დამრტყმელის სხეულის მიმანიშნებელს (ფეხის კუთხე) `0.10 + READING/1000` წამით ადრე. მაღალ დონეზე ეს ნახევარი წამია — შესამჩნევი, მაგრამ არა გარანტია.

**რატომ ასე:** სიზუსტე მოქმედებს *გაფანტვის რადიუსზე*, არა შედეგზე პირდაპირ. ანუ სუსტ მოთამაშესაც შეუძლია კუთხეში მოხვედრა — უბრალოდ ნაკლებად საიმედოდ. უნარი წყვეტს, სტატი მხოლოდ პატიობს შეცდომას. ეს ერთადერთი მიზეზია, რის გამოც RPG-პროგრესია და PvP თანაარსებობს.

---

## 6. პროგრესია და ლიგები

| ლიგა | სტატის ჭერი | MMR |
|---|---|---|
| ბრინჯაო | 40 | < 1100 |
| ვერცხლი | 55 | 1100–1300 |
| ოქრო | 70 | 1300–1500 |
| პლატინა | 85 | 1500–1750 |
| ელიტა | 100 | 1750+ |

ჭერი ნიშნავს, რომ ბრინჯაოში 40-ზე მაღალი სტატი მატჩში 40-მდე ჩამოიჭრება. ლეველი გრძელდება, მაგრამ მატჩი თანაბარი რჩება. ამაღლება ხდება MMR-ით, არა ლეველით.

**სეზონი:** 5 კვირა. ბოლოს MMR ნაწილობრივ ბრუნდება (`new = 1000 + (old − 1000) × 0.5`), ჯილდოები რჩება.

**XP:** ვარჯიში 10–40, მატჩი 25 (მოგება) / 10 (წაგება). ლეველაპზე +3 სტატის ქულა, მოთამაშე თვითონ ანაწილებს.

**ენერგია:** 10 ერთეული, 1 ივსება 12 წუთში. ვარჯიში ხარჯავს 1-ს, ონლაინ მატჩი — 0. ონლაინი არასდროს იბლოკება ენერგიით.

### ვარჯიშის მაგალითები

- `მშრალი ფოთოლი` — გაიტანე ბოძის შიდა მხრიდან, 3-ჯერ 5 ცდაში
- `ცივი სისხლი` — გაიტანე timing ზოლის სრულყოფილ ზონაში, 4-ჯერ ზედიზედ
- `კედელი` — დაიჭირე 3 პენალტი ზედიზედ მზარდი სიჩქარით
- `ორივე კუთხე` — მონაცვლეობით ზედა მარცხენა და ზედა მარჯვენა, 6-ჯერ

---

## 7. ავატარის pipeline — მთლიანი ფოტოდან

მოთამაშე ტვირთავს **მთლიან ფოტოს** (ან სელფის), და სისტემა ქმნის მასზე მიმსგავსებულ, ანიმაციისთვის მზა 3D პერსონაჟს.

```
ფოტო → [მოდერაცია] → [Tier A: image→3D + auto-rig]
                          │ QC გაიარა? ──── კი → glb_url
                          │
                          └─ არა → [Tier B: სახე + პროპორციები + ფერები
                                     მზა rigged მოდელზე] → glb_url
```

### Tier A — სრული მსგავსება

Image→3D სერვისი (Tripo, Meshy-ს კლასის — auto-rigging-ით) ფოტოდან აგებს მთლიან პერსონაჟს: სახე, ვარცხნილობა, ტანსაცმლის ფორმა. შედეგი გადის ავტომატურ ხარისხის შემოწმებას:

- პოლიგონები < 30k (მობილურზე უნდა იმუშაოს)
- rig შეიცავს სტანდარტულ humanoid ჩონჩხს (retarget-ისთვის)
- T-pose ტესტი — კიდურები არ არის შერწყმული ტანთან
- სახის რეგიონს აქვს ტექსტურა (არა ბლანკი)

### Tier B — fallback (და MVP-ის საწყისი წერტილი)

თუ Tier A-ს შედეგი QC-ს ვერ გაივლის (ეს ხშირად მოხდება — image→3D არასტაბილურია), სისტემა იღებს ფოტოდან:
- სახის ტექსტურას → მზა მოდელის თავზე
- სხეულის პროპორციებს (სიმაღლე/აღნაგობა) → მოდელის morph პარამეტრებად
- ჩაცმულობის დომინანტურ ფერებს → ფორმის შეღებვა

მოთამაშე მაინც „საკუთარ თავს“ იღებს, უბრალოდ სტილიზებულს. **ფაზა 3 იწყება Tier B-თი** — ის საიმედოა და ერთ კვირაში მუშაობს. Tier A ემატება მე-4 ფაზის შემდეგ, როცა თამაში უკვე ცოცხალია.

### საერთო წესები

1. ფოტო იტვირთება დროებით bucket-ში
2. Edge Function ამოწმებს: ერთი ადამიანი? უხამსი კონტენტი? ცნობადი უნებართვო პირი არა? (მოდერაციის API)
3. `.glb` ინახება მუდმივ bucket-ში, **ორიგინალი ფოტო იშლება**
4. თამაშში იტვირთება `useGLTF`-ით; ანიმაციები საერთო humanoid ჩონჩხზეა და retarget-ით ჯდება ორივე Tier-ის მოდელზე
5. ავატარის სტატუსი პროფილში: `pending → processing → ready | fallback | rejected`

**პრივატულობა:** ატვირთვამდე აშკარა თანხმობა — „ეს ჩემი ფოტოა ან მაქვს ნებართვა“. ორიგინალი არ ინახება. წაშლის ღილაკი პროფილში, რომელიც `.glb`-საც შლის. ევროპაში სახის/ბიომეტრიული მონაცემი მგრძნობიარე კატეგორიაა და ეს არ არის ფორმალობა.

**კონტრაქტი:** სერვისი ნებისმიერ დროს ჩანაცვლებადია — თამაშისთვის ინტერფეისი მხოლოდ `glb_url`-ია. ხვალ Tripo-ს Meshy-თი შეცვლი და კოდი არ შეიცვლება.

### ავატარი + აღჭურვილობა

აღჭურვილობა (§7ბ) ცალკე მოდელებია, რომლებიც ავატარის ჩონჩხის კვანძებზე ეკვრება (ბუცები → ტერფები, ხელთათმანები → მტევნები). ასე ერთი ბუცის მოდელი ყველა ავატარზე მუშაობს.

---

## 7ბ. აღჭურვილობა და სკინები

ორი მკაცრად გამიჯნული კატეგორია:

### აღჭურვილობა (gear) — მოქმედებს სტატებზე, მხოლოდ მოიპოვება

| სლოტი | მაგალითი | ბონუსი |
|---|---|---|
| ბუცები | „მშრალი ფოთოლი Pro" | +4 სიზუსტე, +2 ეფექტი |
| ხელთათმანები | „კედლის ხელები" | +4 ხელები, +2 რეაქცია |
| სამაჯური | „ცივი სისხლი" | +3 ნერვები |

წესები:
- **gear ფულზე არასდროს იყიდება.** წყაროები: ლიგის ამაღლება, სეზონის ჯილდო, ვარჯიშის სერიები (მაგ. 7 დღე ზედიზედ), მიღწევები
- gear-ის ბონუსი **ემატება სტატს ლიგის ჭერამდე** — ჭერს ვერ გადააჭარბებს. ბრინჯაოში 38 სიზუსტე + 4 ბუცი = 40, არა 42
- სლოტები: ბუცები, ხელთათმანები, 1 აქსესუარი. სამივე ერთად მაქს +10 ჯამური ბონუსი
- იშვიათობა: ჩვეულებრივი / იშვიათი / ლეგენდარული — განსხვავდება ბონუსით *და* ვიზუალით

### სკინები — მხოლოდ ვიზუალი, შეიძლება გაიყიდოს

ფორმები, ბურთის დიზაინები, გოლის ცელებრაციები, კარის ბადის ფერი, სტადიონის თემები. ნულოვანი გავლენა თამაშზე — ამიტომ მონეტიზაცია აქ უსაფრთხოა.

### სქემა

```sql
items (
  id uuid pk, code text unique, kind text,   -- gear | skin
  slot text,          -- boots | gloves | accessory | kit | ball | celebration
  rarity text, stat_bonus jsonb,             -- {"accuracy": 4, "curve": 2}
  model_url text, source text                -- league | season | streak | shop
)
player_items (user_id, item_id, equipped boolean, acquired_at)
```

Equip/unequip მხოლოდ Edge Function-ით — მატჩის დაწყებისას სერვერი ითვლის ეფექტურ სტატებს: `min(base + gear, league_cap)`.

---

## 8. დიზაინის მიმართულება

### კონცეფცია: „ცარიელი წუთი"

პენალტის ვიზუალური არსი არ არის დარტყმა. ეს არის დარტყმამდე მეორე — როცა სტადიონი ჩუმდება, პროჟექტორი თვალს გიბრმავებს და 11 მეტრი უსასრულოდ გრძელია. ინტერფეისი ამ სიჩუმეს უნდა ემსახურებოდეს, არა ხმაურს.

### პალიტრა

```css
--night:      #08120E;  /* ღამის მოედანი, ფონი */
--turf:       #12281F;  /* ბალახის ჩრდილი, სექციები */
--chalk:      #E8EDE6;  /* ცარცი — ტექსტი, ხაზები */
--sodium:     #F2B23E;  /* პროჟექტორის თბილი შუქი — აქცენტი */
--card:       #D8443C;  /* ბარათის წითელი — ხელიდან გაშვება */
--floodlight: #6FA8C7;  /* ცივი ანარეკლი — მეკარის მდგომარეობა */
```

ორი ტემპერატურის შუქი — თბილი ნატრიუმის და ცივი ჰალოგენის — ეს ნამდვილი სტადიონის თვისებაა და უფასოდ გაძლევს ვიზუალურ დაძაბულობას. ამბერი ყოველთვის დამრტყმელია, ცივი ლურჯი ყოველთვის მეკარე. მოთამაშე ამას ერთ მატჩში ისწავლის.

### ტიპოგრაფია

ქართული აქ რეალური შეზღუდვაა — Google Fonts-ის უმეტესობას ქართული გლიფები არ აქვს.

- **Display:** `Noto Sans Georgian` ExtraBold, tracking −0.02em — სათაურები, ანგარიში
- **ციფრები/scoreboard:** `Archivo Expanded` 900, tabular-nums — მხოლოდ ლათინური ციფრებისთვის
- **Body:** `Fira GO` — ქართულიც და ლათინურიც ერთ ოჯახში, ჰუმანისტური, კარგად იკითხება მობილურზე
- სკალა: 12 / 14 / 16 / 20 / 28 / 48 / 88

### ხელმოწერა: „ledger"

რეალურ სერიაში შედეგს ინიშნავენ რიგად — შევსებული და ცარიელი წრეებით. ეს არის აპლიკაციის მთავარი სტრუქტურული ელემენტი და ყველგან მეორდება:

```
●●○●◐        მიმდინარე სერია
●●●●●●●○○○   ლიგის პროგრესი
●●●○○        ვარჯიშის ცდები
```

ერთი ვიზუალური ენა მთელ პროდუქტში. მოთამაშემ ერთხელ უნდა ისწავლოს.

### მოძრაობა

ერთი ორკესტრირებული მომენტი, არა მიმოფანტული ეფექტები:

- swipe-ის შემდეგ ხმა **ქრება** 0.4წმ-ით — მხოლოდ ბურთი
- კამერა დამრტყმელის მხრიდან, სანელებელი 0.9წმ ბურთის ფრენაზე
- გოლზე ბადე ირხევა, ხმა ბრუნდება; გაშვებაზე — სიჩუმე ერთი წამით მეტხანს
- `prefers-reduced-motion` ითიშება ყველა ეს, ლოგიკა უცვლელი რჩება

### რას ვერიდებით

გრადიენტიან ბარათებს, გლასმორფიზმს, ნეონ-მწვანე აქცენტს შავ ფონზე, სტოკ ფეხბურთის ფოტოებს. მოედანი თვითონ არის ვიზუალი — მას სჭირდება სივრცე, არა დეკორაცია.

---

## 9. აშენების გეგმა

### ფაზა 0 — პროტოტიპი (ქსელის გარეშე) 🔑

ერთი გვერდი, ერთი ბრაუზერი, ბოტი მეკარე. კარი, ბურთი, swipe, timing ზოლი, §5-ის ფორმულები.

**ეს ფაზა განსაზღვრავს თამაშის ბედს.** თუ დარტყმა ხელში კარგად არ ჯდება, არც ავატარი და არც ლიგები გადაარჩენს. დაუთმე იმდენი, რამდენიც დასჭირდება, და ითამაშე 50-ჯერ სანამ გააგრძელებ.

### ფაზა 1 — საფუძველი
Supabase პროექტი, სქემა, RLS, auth (Google + email), პროფილი, GitHub რეპო + Vercel deploy.

### ფაზა 2 — ონლაინი
`create-match` / `submit-input` / `resolve-round` Edge Functions, Realtime არხი, ოთახის კოდი და ლინკი, 5+5 სერია, sudden death, დისკონექტის დამუშავება.

### ფაზა 3 — ავატარები (Tier B)
ატვირთვა, მოდერაცია, სახე + პროპორციები + ფერები მზა rigged მოდელზე, Storage, თამაშში ჩატვირთვა default მოდელით fallback-ად.

### ფაზა 4 — პროგრესია
სტატები, XP, ლეველაპი, ქულების განაწილება, ვარჯიშები, ენერგია.

### ფაზა 5 — ლიგები და აღჭურვილობა
MMR (Elo, K=32), ჭერის სისტემა, მატჩმეიკინგის რიგი, სეზონები, ლიდერბორდი. Gear-ის სისტემა (§7ბ): items სქემა, equip, ლიგის ჯილდოები — gear სწორედ აქ იძენს აზრს, რადგან ჯილდოს წყაროები ლიგებია.

### ფაზა 6 — გაპრიალება და Tier A
ხმა, ჰაპტიკა, შედეგის გაზიარება (OG სურათი ავატარით — ეს არის შენი მარკეტინგი), PWA. Image→3D სრული pipeline (Tier A) QC-ით. სკინების მაღაზია — მხოლოდ მაშინ, როცა თამაშს აქტიური მოთამაშეები ჰყავს.

---

## 10. Claude Code-ის პრომპტები

**ფაზა 0:**
> წაიკითხე `SPEC.md`. ჯერ scaffold: Vite + React + TypeScript რეპოს root-ში, დააყენე three, @react-three/fiber, @react-three/drei, zustand, Tailwind; `npm run build` უნდა გადიოდეს, რადგან main ავტომატურად deploy-დება Cloudflare Pages-ზე. მერე ააშენე ფაზა 0: ერთი გვერდი (src/App.tsx). კარი, მოედანი, ბურთი, swipe-ით დამიზნება, timing ზოლი, ბოტი მეკარე. გამოიყენე ზუსტად §5-ის ფორმულები, ცალკე ფაილში `src/lib/physics.ts`, სუფთა ფუნქციებად რომ ტესტირებადი იყოს. ქსელი და ავტორიზაცია არ გჭირდება. ვიზუალი — §8-ის პალიტრა.

**ფაზა 2:**
> ვამატებ ონლაინს. დანერგე §4-ის პროტოკოლი Supabase Edge Functions-ით. `round_inputs`-ს SELECT პოლიტიკა არ უნდა ჰქონდეს — მოწინააღმდეგის არჩევანი კლიენტამდე ვერ უნდა მივიდეს გადაწყვეტამდე. შედეგს ითვლის მხოლოდ სერვერი და აბრუნებს seed-ს, კლიენტი მხოლოდ აანიმაციებს.

**დიზაინისთვის (Claude Design):**
> ეკრანები: მთავარი მენიუ, ოთახის შექმნა/შესვლა, მატჩის ეკრანი ledger-ით, შედეგი, პროფილი სტატებით, ვარჯიშის სია, ლიგის ცხრილი. მიმართულება — §8. „ცარიელი წუთი", ორი ტემპერატურის შუქი, ledger როგორც ერთადერთი პროგრესის ენა.

---

## 11. გადაწყვეტილებები, რომლებიც ჩაწერილია

- შედეგს ითვლის მხოლოდ სერვერი, ყოველთვის
- სიზუსტე მოქმედებს გაფანტვაზე, არა შედეგზე
- ლიგა არის სტატების ჭერი, არა ეტიკეტი — gear-ის ბონუსიც ჭერს ექვემდებარება
- **gear (სტატებიანი ნივთი) ფულზე არასდროს იყიდება; იყიდება მხოლოდ სკინი**
- ორიგინალი ფოტო არ ინახება
- ენერგია არასდროს ბლოკავს ონლაინ თამაშს
- ავატარის სერვისი ჩანაცვლებადია — `glb_url` არის კონტრაქტი
- ავატარი იწყება Tier B-თი; Tier A ემატება მოგვიანებით QC-ის უკან

## 12. ღია საკითხები

- დარტყმის ხანგრძლივობის ბალანსი — მხოლოდ თამაშით დგინდება
- image→3D სერვისის საბოლოო არჩევანი — ფაზა 6-მდე დროა, ბაზარი სწრაფად იცვლება
- ტურნირები და გუნდები — v2
- gear-ის ზუსტი ჯილდოების ცხრილი — ფაზა 5-ში, რეალური ლიგის მონაცემებით

---

## 13. პირველი დღის გეგმა — ტერმინალის გარეშე

მთელი workflow ბრაუზერში/დესკტოპ აპში მიდის: **Claude Code (cloud) → PR → merge → Cloudflare ავტო-deploy → თამაშობ ლინკზე.**

1. **SPEC.md რეპოში** — GitHub-ის ვებ-გვერდიდან: რეპოში „Add file → Upload files", ატვირთე ეს დოკუმენტი სახელით `SPEC.md`, Commit changes.
2. **Cloudflare Pages** — dash.cloudflare.com → Workers & Pages → Create → Pages → Connect to Git → აირჩიე რეპო. Framework preset: Vite, build `npm run build`, output `dist`. პირველი build ჩავარდება (რეპო ჯერ ცარიელია) — ნორმალურია.
3. **Claude Code cloud სესია** — დესკტოპ აპის Code ტაბი ან claude.ai/code → აირჩიე რეპო → გაუშვი ფაზა 0-ის პრომპტი (§10). სესია იზოლირებულ გარემოში მუშაობს და შედეგს PR-ად აბრუნებს.
4. **Merge** — GitHub-ზე გახსენი PR, გადახედე აღწერას, Merge. ორ წუთში Cloudflare მოგცემს `*.pages.dev` ლინკს.
5. **ითამაშე** — დესკტოპზეც და ტელეფონზეც. ჩაინიშნე რა არ ჯდება ხელში: სვაიფი ზედმეტად მგრძნობიარეა? timing ზოლი ჩქარია? ბურთი ნელია? ეს ჩანაწერები ბალანსის სესიის მასალაა.

რაც **არ** კეთდება პირველ დღეს: Supabase, ავტორიზაცია, ავატარი, ქსელი. ფაზა 0 განზრახ ლოკალურია ლოგიკით — ჯერ თამაში უნდა იყოს კარგი, მერე ონლაინ.
